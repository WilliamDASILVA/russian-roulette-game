version: '2'
services:
  back:
    build: apps/back
    command: npm start
    environment:
      - PORT=3000
    volumes:
      - ./apps/back:/app
    ports:
      - 3000:3000
      - 3001:3001
  app:
    build: apps/app
    command: npm start
    environment:
      - PORT=80
    volumes:
      - ./apps/app:/app
    ports:
      - 80:80
